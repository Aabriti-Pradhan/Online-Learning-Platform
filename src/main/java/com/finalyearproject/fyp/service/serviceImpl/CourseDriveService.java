package com.finalyearproject.fyp.service.serviceImpl;

import org.springframework.stereotype.Service;

/**
 * Phase 1 — Google Drive removed. This stub keeps the class present
 * for any remaining references. Safe to delete once all Drive references are gone.
 */
@Service
public class CourseDriveService {
    /** No-op. Local disk storage now handles file organisation. */
    public String createCourseFolder(String name) { return null; }
    public void   deleteCourseFolder(String id)   { }
}
